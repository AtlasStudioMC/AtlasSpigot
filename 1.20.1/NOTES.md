# 1.20.1-specific notes

Same profile as 1.20.2/1.20.4: old-era branding (hardcoded `serverName` field +
`PurpurConfig.serverModName`, no `ServerBuildInfoImpl.java`/manifest `Brand-Name`). Pufferfish's
`dab`/`inactive-goal-selector-throttle` both default `false`, both enabled. Same paper-global/
world-defaults tuning as every track. Clean boot both fresh and tuned, no crashes this time.
